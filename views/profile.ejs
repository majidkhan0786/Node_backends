<!DOCTYPE html>
<html lang="en">

<head>
    <title>Profile Page with Ejs</title>
</head>

<body>
    <h1>Welcome <%= user.name %>
    </h1>
    <h1>Email <%= user.email %>
    </h1>
    <h1>City <%= user.city %>
    </h1>

    <ul>
        <% user.skills.forEach(skill=> { %>
            <li>
                <%= skill %>
            </li>
            <% }); %>
    </ul>
    </h1>
</body>

</html>